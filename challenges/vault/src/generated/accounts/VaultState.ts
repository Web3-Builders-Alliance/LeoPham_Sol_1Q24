/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link VaultState}
 * @category Accounts
 * @category generated
 */
export type VaultStateArgs = {
  maker: web3.PublicKey
  taker: web3.PublicKey
  seed: beet.bignum
  stateBump: number
  vaultKeeper: web3.PublicKey
  vaultBump: number
  createdAt: beet.bignum
  amount: beet.bignum
  lockSeconds: beet.bignum
}

export const vaultStateDiscriminator = [228, 196, 82, 165, 98, 210, 235, 152]
/**
 * Holds the data for the {@link VaultState} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class VaultState implements VaultStateArgs {
  private constructor(
    readonly maker: web3.PublicKey,
    readonly taker: web3.PublicKey,
    readonly seed: beet.bignum,
    readonly stateBump: number,
    readonly vaultKeeper: web3.PublicKey,
    readonly vaultBump: number,
    readonly createdAt: beet.bignum,
    readonly amount: beet.bignum,
    readonly lockSeconds: beet.bignum
  ) {}

  /**
   * Creates a {@link VaultState} instance from the provided args.
   */
  static fromArgs(args: VaultStateArgs) {
    return new VaultState(
      args.maker,
      args.taker,
      args.seed,
      args.stateBump,
      args.vaultKeeper,
      args.vaultBump,
      args.createdAt,
      args.amount,
      args.lockSeconds
    )
  }

  /**
   * Deserializes the {@link VaultState} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [VaultState, number] {
    return VaultState.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link VaultState} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<VaultState> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find VaultState account at ${address}`)
    }
    return VaultState.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      '7Qc3nfhGh6tJgRJMVjDcek83SD4pLnCr5vbYC4Rn7Sxs'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, vaultStateBeet)
  }

  /**
   * Deserializes the {@link VaultState} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [VaultState, number] {
    return vaultStateBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link VaultState} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return vaultStateBeet.serialize({
      accountDiscriminator: vaultStateDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link VaultState}
   */
  static get byteSize() {
    return vaultStateBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link VaultState} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      VaultState.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link VaultState} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === VaultState.byteSize
  }

  /**
   * Returns a readable version of {@link VaultState} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      maker: this.maker.toBase58(),
      taker: this.taker.toBase58(),
      seed: (() => {
        const x = <{ toNumber: () => number }>this.seed
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      stateBump: this.stateBump,
      vaultKeeper: this.vaultKeeper.toBase58(),
      vaultBump: this.vaultBump,
      createdAt: (() => {
        const x = <{ toNumber: () => number }>this.createdAt
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      amount: (() => {
        const x = <{ toNumber: () => number }>this.amount
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      lockSeconds: (() => {
        const x = <{ toNumber: () => number }>this.lockSeconds
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const vaultStateBeet = new beet.BeetStruct<
  VaultState,
  VaultStateArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['maker', beetSolana.publicKey],
    ['taker', beetSolana.publicKey],
    ['seed', beet.u64],
    ['stateBump', beet.u8],
    ['vaultKeeper', beetSolana.publicKey],
    ['vaultBump', beet.u8],
    ['createdAt', beet.i64],
    ['amount', beet.u64],
    ['lockSeconds', beet.i64],
  ],
  VaultState.fromArgs,
  'VaultState'
)
